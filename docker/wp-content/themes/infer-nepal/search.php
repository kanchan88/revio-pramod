<?php
/* All search defaults to software CPT (handled in functions.php pre_get_posts).
 * Reuse the archive listing layout — same filters apply. */
include get_theme_file_path('archive-software.php');
